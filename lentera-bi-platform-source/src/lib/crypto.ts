// ponytail: demo credential masking — real encryption via env var if needed later
export function encrypt(_text: string): string {
  return '****';
}

export function decrypt(_encoded: string): string {
  return '****';
}
